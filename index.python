BFSالگوریتم 
def bfs(start,graph)
    queue=[start]
    visited={start}
    while queue:
        vertex=queue pop(0)

        frone in graph[vertex]
        if ne not invisited:

       visited.add(ne)
    queue.append(ne)

    return visited
    mohamad amin ghazanfri